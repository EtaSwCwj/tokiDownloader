# tokiDownloader GUI 개발 계획서

## 1. 문서 목적

이 문서는 `C:\gitproject\tokiDownloader`의 Windows GUI를 현재 구현 상태에서 계속
발전시키기 위한 실행 계획서다. 요구사항과 세부 기술 원칙은
`CLAUDE_GUI_IMPLEMENTATION_BRIEF.md`를 기준으로 하며, 이 문서는 **무엇을 어떤 순서로
구현하고 어디까지 검증해야 완료인지**를 정의한다.

문서 간 우선순위는 다음과 같다.

1. 사용자가 현재 대화에서 직접 지시한 내용
2. 이 개발 계획서의 단계와 완료 조건
3. `CLAUDE_GUI_IMPLEMENTATION_BRIEF.md`의 상세 요구사항과 기술 원칙
4. `README.md`의 공개 사용법

## 2. 최종 목표

사용자가 URL과 저장 폴더를 지정하면 작품 단위로 다운로드를 관리하고, 대표 이미지,
메타데이터, 회차 상태, 실행 이력과 로그를 한 화면에서 확인할 수 있는 실제 Windows
데스크톱 프로그램을 완성한다.

핵심 목표는 다음과 같다.

- 더블클릭으로 실행되는 PyQt6 Windows GUI
- `[작가][그룹] 제목` 폴더 규칙과 `metadata.json`, 대표 이미지 보존
- 같은 작품은 하나의 목록 행으로 관리하고 실행 기록만 별도로 누적
- 전체 재검사 시 사이트의 현재 회차를 다시 수집하고 기존 파일은 건너뛰기
- 기본 headless 브라우저 실행과 필요할 때만 보이는 브라우저 진단 모드
- 수천~수만 작품에서도 빠른 시작, 검색, 정렬과 스크롤
- 모든 GUI 기능을 같은 공용 서비스와 CLI를 통해 자동 검증 가능
- 오류 발생 시 GUI 로그, 파일 로그, 상태 JSON과 화면 캡처로 원인을 재현 가능

## 3. 현재 기준선

다음 기능은 현재 구현된 기준선으로 취급하고 회귀되지 않게 유지한다.

- `start-gui.cmd`와 `toki-cli.cmd`
- URL, 회차 범위, 저장 폴더 입력과 작업 대기열
- 작업 중지, 작품 전체 재검사, 저장 폴더 열기
- 원본 링크와 작품명 복사
- GUI/파일 로그, 로그 복사와 삭제
- CLI를 통한 GUI 실행, 표시, 종료, 상태 조회와 화면 캡처
- 창 위치, 크기와 최대화 상태 기억
- Chrome 기본 headless 실행과 선택적 `--show-browser`
- 작품 식별자 `사이트 종류:작품 ID`에 따른 중복 통합
- SQLite 작업 기록, 페이지 단위 로딩, 가상화 목록과 썸네일 LRU 캐시
- 같은 작품 재실행 시 새 행을 추가하지 않고 기존 행 갱신
- 단일 클릭 선택, 더블클릭 폴더 열기, 우클릭 실제 기능 메뉴

현재 변경 사항을 무조건 되돌리거나 기존 다운로드 폴더를 재구성하지 않는다. 작업을 시작할
때 실제 Git 상태와 관련 파일을 먼저 확인하고, 사용자 변경과 충돌하면 보존 가능한 방법을
선택한다.

## 4. 개발 불변 원칙

### 4.1 기능 구현 순서

모든 사용자 기능은 아래 순서로 구현한다.

1. 공용 application service 또는 저장소 기능
2. CLI 명령과 기계 판독 가능한 결과
3. 단위 또는 통합 테스트
4. GUI 버튼, 메뉴, 대화상자 연결
5. CLI 재검증과 GUI 직접 캡처 확인
6. README와 이 계획서의 상태 갱신

GUI와 CLI가 같은 기능을 별도로 중복 구현하지 않는다. GUI에 보이는 버튼과 메뉴는 반드시
CLI에서도 실행하거나 결과를 검증할 수 있어야 한다.

### 4.2 데이터 안전

- 기존 다운로드 파일, 폴더와 메타데이터를 자동 삭제하지 않는다.
- 기록 제거와 실제 파일 삭제를 서로 다른 명령으로 분리한다.
- 파일 삭제, 대량 이동, 되돌리기 어려운 데이터 마이그레이션은 사용자 확인 없이 실행하지
  않는다.
- 폴더 이동은 대상 경로 충돌, 디스크 여유 공간, 같은 볼륨 여부와 실패 시 복구 방법을
  확인한다.
- 중단과 재시도는 멱등적으로 처리하며 같은 이벤트가 반복돼도 상태가 손상되지 않게 한다.

### 4.3 성능과 동시성

- Qt 메인 스레드에서는 네트워크, 대량 파일 검사, 이미지 디코딩과 데이터베이스 전체
  조회를 하지 않는다.
- 목록은 model/view 가상화와 SQLite 페이지 조회를 유지한다.
- I/O 작업은 제한된 스레드 풀, CPU 집약 작업은 제한된 프로세스 풀로 분리한다.
- 브라우저 수, 작품 동시 처리 수와 작품 내부 이미지 동시 다운로드 수를 독립적으로
  제한한다.
- 무제한 큐를 만들지 않고 취소와 백프레셔를 제공한다.
- 성능 개선은 100, 1,000, 10,000, 100,000개 합성 데이터로 측정한다.

### 4.4 오류 처리와 관측성

- 오류를 성공이나 단순 완료 상태로 표시하지 않는다.
- 오류에는 작업 ID, 작품 ID, 단계, 사용자용 메시지와 진단용 원인을 남긴다.
- 비동기 CLI 기능은 작업 ID를 반환하고 `status --job ID --json`으로 추적 가능하게 한다.
- 로그는 크기 제한과 순환 정책을 사용하고 GUI에는 최근 행만 유지한다.
- 새 기능은 필요한 경우 CLI 화면 캡처와 상태 JSON만으로도 결과를 확인할 수 있게 한다.

## 5. 단계별 개발 로드맵

각 단계는 선행 단계의 완료 조건을 통과한 뒤 진행한다. 단, 서로 독립적이고 회귀 위험이
낮은 작은 항목은 같은 커밋 묶음에서 처리할 수 있다.

### 단계 0. 기준선 고정과 자동 검증

목표: 이후 기능을 빠르게 추가해도 기존 동작이 깨졌는지 한 번에 확인할 수 있게 한다.

- [x] 현재 Git 변경 사항을 기능별로 검토하고 불필요한 산출물이 추적되지 않게 정리
- [x] 핵심 모델, 작품 키, 재시도 범위, SQLite upsert 단위 테스트 추가
- [x] IPC 요청과 CLI JSON 응답 테스트 추가
- [x] `toki-cli.cmd self-test --json` 구현
- [x] 네트워크 없는 빠른 테스트와 실제 사이트 1화 통합 테스트 분리
- [x] GUI 시작 → CLI 제어 → 상태 확인 → 캡처 → 종료 자동 점검 구현
- [x] 테스트 결과와 실패 로그 저장 위치를 README에 기록

구현 메모: 기본 `self-test`는 네트워크를 사용하지 않으며, 실제 사이트 검증은 README의
명시적 `download --direct --start 1 --last 1` 명령으로 분리했다. GUI 버튼 경로는
`self-test --via-gui --json`으로 자동 실행하고 완료 상태를 조회할 수 있다.

완료 조건:

- 한 명령으로 로컬 핵심 기능 검증이 끝난다.
- 실패한 검사 이름과 원인이 JSON 및 로그에 표시된다.
- 기존 Node CLI 구문 검사와 Python 컴파일 검사가 함께 통과한다.

### 단계 1. 작품 목록 관리

목표: 작품이 수백~수천 개 쌓여도 원하는 항목을 빠르게 찾고 안전하게 정리한다.

- [x] 제목, 작가, 그룹과 작품 ID 검색
- [x] 전체/대기/실행/완료/오류/중단 상태 필터
- [x] 최근 갱신, 제목, 진행률 정렬
- [x] 고정 작품과 사용자 색상 태그
- [x] 선택 작품 기록 제거
- [x] 완료/오류 기록 일괄 정리
- [x] 목록 새로고침과 썸네일 캐시 갱신
- [x] 우클릭 메뉴와 상단 메뉴에 실제 구현 기능 연결

구현 메모: `list --query --status --sort --json`은 GUI 실행 여부와 관계없이 같은 SQLite
조회 서비스를 사용한다. `--apply-gui`를 추가하면 GUI 필터 컨트롤과 가상화 목록에도
조건을 적용하며 `status --json`의 `listFilter`에서 결과를 검증할 수 있다.
고정과 색상 태그는 작품 레코드에 영구 저장되고, `pin`/`tag` CLI 및 우클릭 메뉴가 같은
서비스를 사용한다. 고정 작품은 선택한 정렬 방식과 관계없이 먼저 표시된다.
`remove-record --yes`와 우클릭 메뉴의 기록 제거는 SQLite 작품 레코드만 삭제하고 실제
다운로드 폴더와 파일은 보존한다. 대기·실행 중 기록은 제거할 수 없다.
일괄 정리도 동일한 파일 보존 계약과 명시적 확인을 사용한다. `refresh-list`는 SQLite
페이지 재조회와 썸네일 메모리 캐시 초기화를 함께 수행한다.

필수 CLI 예시:

```text
list --query TEXT --status STATUS --sort updated --limit 200 --json
pin --job ID --on|--off
tag --job ID --color COLOR
remove-record --job ID
cleanup-records --status completed --before DATE
refresh-thumbnail --job ID
```

완료 조건:

- 기록 제거는 다운로드 파일을 삭제하지 않는다.
- 필터와 검색은 메모리 전체 순회가 아니라 인덱스가 있는 SQLite 쿼리를 사용한다.
- 10,000개 합성 기록에서 첫 페이지 조회와 검색 성능 목표를 통과한다.

### 단계 2. 작품 상세 정보와 실행 이력

목표: 작품 행 하나에서 메타데이터, 메모와 과거 실행 결과를 추적한다.

- [x] `runs` 테이블을 추가하여 작품과 실행 기록을 1:N으로 분리
- [x] 요청 범위, 실제 검사 회차, 시작/종료 시간, 결과, 오류와 PID 기록
- [x] 작품 상세 패널 또는 대화상자
- [x] 작가, 그룹, 제목, 원본 URL, 저장 경로와 메타데이터 표시
- [x] 사용자 메모 추가·수정
- [x] 실행 이력 페이지 조회와 상세 로그 연결
- [x] 기본 브라우저에서 원본 작품 페이지 열기
- [x] 대표 이미지 원본 열기와 다시 받기

필수 CLI 예시:

```text
info --job ID --json
runs --job ID --limit 100 --json
run-info --run ID --json
run-logs --run ID --tail 500 --json
run-log --run ID
set-note --job ID --text TEXT
open-source --job ID
open-cover --job ID
refresh-metadata --job ID
```

완료 조건:

- 같은 작품을 여러 번 실행해도 작품 행은 하나이고 실행 이력만 누적된다.
- 실행 이력이 많아져도 상세 창은 페이지 단위로 로딩된다.
- 데이터베이스 마이그레이션 전후 레코드 수와 작품 키가 검증된다.

2026-08-07 검증: 임시 SQLite DB에 실행 이력 10,000건을 일괄 저장한 뒤 중간 페이지
100건을 조회했다. 현재 개발 PC 기준 저장 185.5ms, 개수 계산과 페이지 조회 합계 53.0ms였고
실제 GUI는 한 번에 최대 100건만 표에 표시한다. 이 수치는 환경별 성능 보장이 아니라
페이지·인덱스 구조가 전체 위젯 생성 없이 동작하는지 확인한 기준값이다.

2026-08-07 메타데이터 전용 통합 검증: `manhwa/34360`을 격리 경로에
`-metadata-only -content-path`로 실행해 전체 272회 확인, 선택 0회, 처리 0회였으며 결과물은
`metadata.json`과 `cover.png`만 생성됐다. 회차 시작·이미지 저장 이벤트는 발생하지 않았다.

### 단계 3. 다운로드 큐와 제어 강화

목표: 여러 작품을 안정적으로 예약하고 개별적으로 제어한다.

- [x] 선택 작업 중지와 대기 작업 취소를 작업 ID 기준으로 분리
- [x] 대기열 순서 변경과 우선순위
- [x] 일시정지·계속 기능의 실제 의미 정의 및 구현
- [x] 작품 동시 다운로드 수 설정
- [x] 작품 내부 이미지 동시 다운로드 수 설정과 안전한 상한
- [x] 신규 회차만 검사, 전체 재검사, 지정 범위 검사를 명확히 분리
- [x] 앱 재시작 시 실행 중이던 작업을 중단됨으로 복구
- [x] 자동 재시도 횟수와 지수 백오프 설정
- [x] Cloudflare/인증 필요 상태를 일반 네트워크 오류와 분리

필수 CLI 예시:

```text
stop --job ID
cancel --job ID
queue move --job ID --before OTHER_ID
pause --job ID
resume --job ID
rescan --job ID --mode full|new|range
set-concurrency --works N --images N
```

완료 조건:

- 제어 명령은 선택 상태가 아니라 정확한 작업 ID를 대상으로 한다.
- 중지 후 해당 작업이 만든 Node/Chrome 프로세스 트리만 종료된다.
- 재시도와 재검사가 이미 받은 정상 파일을 불필요하게 다시 받지 않는다.

2026-08-07 작품 동시성 구현: 기본값 1, 상한 4로 설정하고 작품별 `QProcess`와
표준 출력·오류 버퍼, 실행 이력, 중지·일시정지 플래그를 분리했다. 실행 작품 수가
설정값보다 작아질 때마다 대기열을 자동으로 채우며, GUI·CLI 상태에서 모든 현재 작업과
프로세스 PID를 작업 ID별로 확인할 수 있다. 단위 테스트는 3개 대기 작업에서 상한 2만큼의
서로 다른 프로세스 컨텍스트가 시작되고 1개가 대기에 남는지 검증한다.

2026-08-07 재검사 모드 분리: `new`는 `.toki-state.json`의 완료 회차를 제외하고,
`full`은 전체 회차 페이지를 다시 확인하며, `range`는 시작·마지막 구간만 선택한다.
기존 파일 건너뛰기는 모든 모드에서 유지하고 기존 폴더는 회차 접두어로 최초 상태를
가져온다. Python 계약·CLI 테스트와 Node 회차 선택 테스트 4건을 자체 점검에 포함한다.
실제 통합 검증에서 기존 17개 회차를 모두 받은 `manhwa/33648`에 `new`를 실행해
전체 17개 발견, 0개 선택, 0개 처리, 종료 코드 0과 `download_new` 실행 이력을 확인했다.

2026-08-07 재시작 복구 보강: GUI에 로딩된 첫 200건만 확인하던 로직을 SQLite
전체 `대기/실행 중/일시정지` 상태를 한 트랜잭션에서 복구하는 서비스로 변경했다.
해당 작품과 실행 이력은 중지됨, 종료 시간, 복구 사유를 같이 기록하며 `status`의
`startupRecovery`에서 복구 개수와 ID를 확인할 수 있다. 1,000개 완료 기록 뒤로 밀려 첫
페이지에 없는 중단 작업도 복구되는지 자동 테스트한다.

2026-08-07 자동 재시도: 기본 2회, 기본 대기 2초로 설정했고 재시도별 대기는
2배씩 증가하며 300초를 넘지 않는다. 성공·사용자 중지·상한 소진은 재시도하지 않고,
`재시도 대기`도 작품별 활성 컨텍스트로 유지해 ID 기반 중지가 가능하다. GUI·CLI에서
횟수 0~5와 기본 대기 1~60초를 설정하고 실행 이력에 시도 횟수를 기록한다.

2026-08-07 오류 분류 구현: 다운로더가 Cloudflare·CAPTCHA·403, 요청 제한, 일반
네트워크, 사이트 선택자 변경, 파일 시스템 오류를 구조화 이벤트로 전달한다. 인증 필요와
비재시도 오류는 지수 백오프 대상에서 제외하고 작품 상태, 실행 이력, GUI 상세와 CLI
`run-info`에 분류를 보존한다. 인증 대기에는 60초 상한을 두어 무한 대기를 방지하며,
개인 브라우저 프로필·쿠키 사용이나 접근 제한 우회는 구현하지 않는다.

### 단계 4. 다운로드 폴더와 파일 도구

목표: 저장 결과를 GUI 안에서 확인하고 안전하게 관리한다.

- [x] 작품 폴더 변경 및 이동
- [x] 폴더명 미리보기와 충돌 검사
- [x] 작품 폴더의 메타데이터 다시 생성
- [x] 보유 회차와 누락/손상 파일 검사
- [x] 이미지 미리보기
- [x] 지원 이미지 형식 변환 기능 검토 및 구현
- [x] 파일 작업 진행률, 취소와 실패 복구

필수 CLI 예시:

```text
move-folder --job ID --output PATH --dry-run --json
rebuild-metadata --job ID --json
verify-files --job ID --json
preview --job ID --episode EPISODE
convert-images --job ID --format FORMAT --dry-run --json
convert-images --job ID --format FORMAT --execute --yes --progress-json
cancel-conversion --job ID
```

완료 조건:

- 이동과 변환은 항상 `--dry-run`으로 예상 대상을 먼저 검증할 수 있다.
- 원본 삭제가 필요한 동작은 별도의 명시적 옵션과 사용자 확인 없이는 실행되지 않는다.
- 작업 도중 실패해도 데이터베이스 경로와 실제 폴더 상태가 불일치한 채 성공 처리되지 않는다.

2026-08-07 작품 폴더 이동: 새 저장 루트 아래에 기존 사이트/작품 폴더명을 유지하는
목적지를 계산하고 동일 경로·기존 목적지·작품 폴더 내부 이동·실행 중 작업을 거부한다.
CLI는 기본 `dry-run`이며 실제 이동에는 `--execute --yes`를 모두 요구한다. GUI 우클릭
메뉴도 목적지 미리보기와 확인을 거친다. 임시 한글·공백 경로에서 회차 파일 보존과
표지·메타데이터·작품 DB 경로 갱신을 자동 테스트했으며 실제 사용자 폴더는 이동하지 않았다.

2026-08-07 로컬 메타데이터 재생성: 작품 DB와 `[작가][그룹] 제목` 폴더명으로 핵심
필드를 복구하고 읽을 수 있는 기존 설명·장르 등의 확장 필드는 보존한다. 기본은
`dry-run`이며 실제 쓰기에는 `--execute --yes` 또는 GUI 확인이 필요하다. 기존 JSON은
`.bak`으로 보존하고 임시 파일을 원자 교체하며, 한글 경로의 임시 작품에서 백업·재생성·DB
재수화를 검증했다. 실제 사용자 작품의 metadata.json은 변경하지 않았다.

2026-08-07 보유 파일 검사: 완료 상태 대비 누락·중복 회차, 빈 회차, 0바이트 이미지,
확장자별 이미지 서명 불일치와 메타데이터/상태 JSON 손상을 읽기 전용으로 분류한다.
문제 상세는 기본 500건으로 제한하고 CLI 종료 코드 0/2로 정상/문제 발견을 구분한다.
GUI는 동일 CLI를 별도 프로세스로 실행해 메인 스레드의 대량 파일 I/O를 피하며 결과 전용
창을 표시한다. 1,000개 회차·1,000개 이미지의 임시 작품 자동 테스트와 실제 작품
17개 회차·336개 이미지 검사를 수행했고 모두 문제 0건을 확인했다.

2026-08-07 이미지 미리보기: 회차 목록과 이미지 경로를 자연 숫자 정렬하고 기본 200장,
최대 1,000장으로 페이지 조회한다. CLI JSON과 GUI가 같은 목록 서비스를 사용하며 GUI는
목록 조회를 별도 프로세스에서, 선택 이미지 축소 디코딩을 최대 2개 Qt 작업 스레드에서
수행한다. 실제 작품 1회차의 17장 목록과 첫 이미지 표시, 회차 선택 및 원본 열기 UI를
캡처로 확인했다.

2026-08-07 이미지 형식 변환: Pillow를 별도 `requirements-image-tools.txt` 선택
의존성으로 분리하고 JPG·PNG·WebP 변환을 구현했다. 기본은 `dry-run`이고 실제 실행에는
`--execute --yes` 또는 GUI 재확인이 필요하다. 결과는 `_converted/형식` 아래에 회차
구조를 유지해 생성하며 원본을 덮어쓰거나 삭제하지 않는다. 같은 결과는 건너뛰고 같은
이름의 서로 다른 원본 확장자는 충돌 없는 대상명을 만든다. 투명 PNG→JPEG, 한글 경로,
원본 바이트 보존과 재실행 건너뛰기를 임시 작품에서 자동 검증했다. 실제 사용자 작품은
336장 WebP 변환 계획만 확인했고 출력 폴더를 생성하지 않았다.

2026-08-07 파일 작업 진행률·취소·복구: 이미지 변환 CLI가 장별 진행 이벤트와 최종
결과를 JSON Lines로 출력하고 GUI는 별도 프로세스 출력을 실시간 반영한다. GUI 중지 버튼과
`cancel-conversion` CLI는 동일한 제어 경로를 사용한다. 대상 이미지는 `.tmp`에 완전히 쓴
뒤 원자 교체하며, 중단으로 남은 임시 파일은 다음 실행에서 제거하고 이미 완성된 결과는
건너뛴다. 서비스 취소 후 재실행, 손상 이미지 실패와 부분 파일 제거, CLI 이벤트 순서,
GUI 실행 인자와 취소의 멱등성을 자동 테스트했다. 실제 사용자 작품에는 변환을 실행하지
않았다.

### 단계 5. 설정과 사용자 경험

목표: 반복 사용에 필요한 설정을 GUI에서 이해하기 쉽게 관리한다.

- [x] 설정 대화상자와 각 설정의 CLI 조회·변경
- [x] 저장 폴더, 동시성, 재시도, 로그 보존과 브라우저 표시 설정
- [x] 작업 행 높이와 정보 밀도 선택
- [x] 밝은/어두운 테마와 시스템 테마 대응
- [x] 완료 알림과 선택적 트레이 동작
- [x] 빈 화면, 로딩, 오류와 작업 없음 상태 정리
- [x] 키보드 탐색과 단축키
- [x] 고해상도 DPI 및 여러 모니터에서 창 복원 검증

완료 조건:

- 설정은 다음 실행에도 유지되고 잘못된 값은 안전한 기본값으로 복구된다.
- 흰 배경/흰 글씨 같은 테마 대비 회귀가 없다.
- 모든 설정 변경을 CLI와 `status --json`으로 확인할 수 있다.

2026-08-07 일반 설정 기반: `도구 → 설정...`에 일반·네트워크·고급 탭을 추가하고 기본
저장 폴더, 브라우저 표시, 로그 패널, 작품/이미지 동시성, 재시도 횟수·대기와 로그 파일
크기·백업 수를 한 번에 저장한다. `settings`, `settings --show-gui --tab ...`,
`set-settings`가 같은 설정 서비스와 IPC를 사용하며 전체 값은 `status --json`에도
포함한다. 잘못된 기존 설정은 범위별 기본값으로 복구하고 config.json은 임시 파일 뒤
원자 교체한다. 로그 순환은 1~100 MiB와 1~10개 백업 범위를 실제 적용한다. 자동 테스트
73개와 각 설정 탭의 GUI 캡처로 밝은 테마 대비를 확인했다.

2026-08-07 목록 정보 밀도: 디스플레이 설정에 `편안하게` 92px 카드와 `간략하게`
66px 카드를 추가했다. 간략 모드는 표지와 별도 진행 막대를 생략하는 대신 상태, 제목,
핵심 회차 정보와 진행률을 유지한다. 설정 변경은 전체 모델 재생성 없이 delegate의 행
크기와 viewport 배치만 갱신하며 `set-settings --row-density`로 동일하게 제어한다.

2026-08-07 테마 대응: `system`, `light`, `dark` 설정을 공용 설정 서비스와
`set-settings --theme`에 추가했다. 메인 위젯과 메뉴·입력·표·로그·설정/상세 대화상자는
공통 색상 토큰을 사용하고 커스텀 작품 카드 delegate도 선택·본문·보조 글자·진행률 색을
테마별로 그린다. 시스템 모드는 Qt 애플리케이션 팔레트 밝기를 사용하며 실행 중 팔레트
변경 이벤트도 반영한다. dark 메인·설정, light 설정, 현재 Windows system 설정을 각각
CLI 화면 캡처로 확인했고 최종 사용자 설정은 system으로 복원했다.

2026-08-07 Windows 콘솔 숨김: GUI가 직접 실행하던 Node·Python `QProcess`를
`CREATE_NO_WINDOW`와 `SW_HIDE`를 사용하는 비동기 프로세스 어댑터로 교체했다. stdout,
stderr, PID, 중지와 완료 신호는 기존 GUI 계약을 유지한다. 프로그램 자체도
`start-gui.vbs`로 열면 시작용 명령 프롬프트가 나타나지 않는다. 기존 `.cmd` 실행기는
초기 설치 진단과 호환성을 위해 유지한다.

2026-08-07 완료 알림·트레이: 트레이 사용, 닫기/최소화 시 숨김, 완료 알림과 오류 알림을
각각 영속 설정으로 추가했다. 트레이 메뉴는 창 표시·숨김, 실행/대기 수와 안전한 종료를
제공한다. 작업의 최종 재시도까지 끝난 뒤 완료·오류·인증 필요 상태만 알리며 사용자 중지는
알리지 않는다. `tray status|show|hide|notify`, `set-settings`와 `status --json`으로 모든
동작과 상태를 GUI 없이 확인할 수 있다. 트레이가 없거나 비활성일 때는 알림·숨기기를
명확한 실패로 반환하고 다운로드 동작에는 영향을 주지 않는다.

2026-08-07 작품 목록 화면 상태: 공용 상태 판정 서비스가 `loading`, `error`, `empty`,
`no_results`, `content`를 구분하고 상태별 제목·설명·복구 동작을 반환한다. GUI는 작품 목록과
상태 패널을 전환하며 URL 입력 이동, 필터 초기화와 다시 시도 버튼을 제공한다. DB 조회 실패는
성공이나 빈 목록으로 숨기지 않고 로그와 오류 패널에 남긴다. `list-state --json`, GUI 미리보기,
`refresh-list`와 `status --json`의 `listViewState`로 같은 상태를 제어·검증할 수 있다. Python
78건과 Node 7건을 통과했고 `logs/list-state-no-results.png`, `logs/list-state-error.png`에서
어두운 테마의 문구·버튼 대비를 직접 확인했다.

2026-08-07 키보드 탐색·단축키: 17개 기본 단축키를 공용 카탈로그로 정의해 QAction과
`shortcuts --json`이 같은 키·동작·대응 CLI를 사용한다. `Ctrl+L`, `Ctrl+F`, `F6`으로 주요
영역을 이동하고 목록 Enter는 상세 정보, `Ctrl+Shift+Up/Down`은 이전·다음 작품을 순환
선택한다. `focus --target ...`과 `status --json`의 `keyboard`로 포커스·선택 행·작업 ID를
검증하며 GUI 도움말 표도 CLI로 열고 닫는다. Python 81건을 통과했고
`logs/keyboard-shortcuts.png`, `logs/keyboard-navigation.png`에서 안내창과 선택 강조를 직접
확인했다.

2026-08-07 DPI·다중 모니터 복원: Qt 논리 좌표와 모니터 이름·배율·상대 위치를 저장해
정상적인 음수 좌표를 보존하고, 저장된 모니터가 분리됐거나 창이 전부 화면 밖에 있을 때만
주 화면의 보이는 영역으로 보정한다. `window --screen NAME --center --safe`와
`status --json`의 `window.screens`, `screenName`, `screenDpr`, `onScreen`으로 같은 경로를
제어·검증한다. 실제 3개 모니터 `X38 P` 125%, `G4309VX_D` 150%, `HDMI` 200%에서 창 이동,
고배율 목록 재배치와 원래 위치 복원을 확인했고 `logs/window-primary-125.png`,
`logs/window-side-150.png`, `logs/window-hdmi-200.png`를 캡처했다. Python 83건을 통과했다.

### 단계 6. 대규모 성능과 안정성

목표: 장기간 누적된 작업과 로그에서도 UI 반응성을 유지한다.

- [x] 상태 이벤트 병합과 UI 갱신 빈도 제한
- [x] SQLite 인덱스와 쿼리 계획 검증
- [x] 썸네일 디스크 캐시 정리 정책
- [x] 로그 순환과 오래된 실행 이력 보존 정책
- [x] 제한형 I/O 스레드 풀과 CPU 프로세스 풀
- [ ] 큐 백프레셔와 메모리 상한
- [x] 100~100,000개 합성 데이터 벤치마크 명령
- [ ] 장시간 실행, 강제 종료와 재시작 복구 테스트

완료 조건:

- 10,000개 기록에서 첫 화면이 목표 시간 2초 이내에 표시된다.
- 스크롤 비용이 전체 작업 수에 선형 비례하지 않는다.
- 유휴 상태에서 작업 수 증가만으로 지속적인 CPU 사용이 발생하지 않는다.
- 성능 결과가 JSON 또는 파일 보고서로 남는다.

2026-08-07 SQLite 목록 진단: 고정 작품을 우선한 갱신일·제목·진행률 정렬과 각 상태 필터
조합을 덮는 6개 복합 인덱스를 추가하고 안정적인 `job_id` 보조 정렬을 사용한다.
`performance audit --json`, `performance audit --show-gui`와 도구 메뉴의
`목록 성능 진단...`이 공용 `EXPLAIN QUERY PLAN` 서비스를 사용한다. 실제 사용자 DB에서
6/6 조회가 인덱스를 사용하고 임시 B-tree 정렬은 0건, 진단은 약 11ms였으며
`logs/performance-diagnostics.png`에서 GUI 결과를 확인했다. Python 85건을 통과했다.

2026-08-07 합성 목록 벤치마크: `performance benchmark`가 사용자 DB와 분리된 임시 WAL
데이터베이스에 100, 1,000, 10,000, 100,000개를 누적 생성해 첫 200개와 세 정렬·상태 필터,
메모리 변화와 저장 공간을 측정하고 `logs/performance-benchmark.json`을 원자 저장한다.
임시 DB는 완료 후 제거한다. GUI 진단창 버튼과 `--via-gui`, `status --json`이 같은 숨김
백그라운드 실행을 제어한다. 실측 100,000개 첫 화면은 CLI 16.1ms, GUI 17.8ms로 2초 목표를
통과했고 전체 CLI 측정은 약 6.3초, 메모리 순증은 약 11MB였다.
`logs/performance-benchmark-gui.png`에서 완료 결과를 확인했고 Python 88건을 통과했다.

2026-08-07 이벤트 병합: 고빈도 `image_saved` 이벤트는 작품별 최신 상태만 100ms 간격으로
카드·요약·DB 저장 예약에 반영한다. 메타데이터, 회차 전환, 오류와 완료 이벤트는 즉시
렌더하고 실행 이력도 바로 저장한다. 공용 `downloader_event_update_policy`를 GUI와
`performance event-policy --json`이 함께 사용하며 `status --json`의 `eventUpdates`에
수신, 대기, 병합, 플러시와 실제 렌더 횟수를 노출한다. 100개 연속 이미지 이벤트가 1회
렌더로 병합되고 종료 시 대기 상태가 강제 반영되는 테스트를 포함해 Python 91건을 통과했다.

2026-08-07 썸네일 캐시: 원본 표지의 절대 경로·크기·수정 시각과 50×66 크기를 SHA-256
키로 삼아 `.cache/thumbnails`에 축소 PNG를 원자 저장한다. 메모리 LRU는 이 파일을 읽어
원본 재디코딩을 줄이며 원본 변경 시 자동으로 새 키를 쓴다. 앱 시작과 도구 메뉴,
`thumbnail-cache status|cleanup --execute`가 공용 정리 서비스를 사용해 2,000개,
256 MiB, 90일 상한과 불완전 파일을 관리한다. 원본 표지·다운로드 파일은 정리 대상이 아니다.
실제 GUI에서 표지 2개가 디스크·메모리 캐시에 생성되고 상태 JSON에 반영됨을 확인했으며,
한글 경로와 미리보기/실행 정리 테스트를 포함해 Python 93건을 통과했다.

2026-08-07 로그·실행 이력 보존: 기존 설정형 로그 순환의 현재 파일·백업·전체 용량을
`retention status --json`과 GUI 상태에 노출한다. 실행 이력은 기본 작품별 500건·365일을
상한으로 삼되 각 작품의 최신 1건과 모든 활성 상태를 보존한다. `cleanup-runs`는 기본
미리보기이며 `--execute` 또는 도구 메뉴 확인 뒤에만 오래된 `runs` 행을 제거한다. 작품
레코드와 다운로드 파일은 대상이 아니다. 실제 DB는 로그 1개 약 73KB, 정리 후보 0건이었고
최신·활성·유일한 오래된 기록 보존과 CLI/GUI 실행 계약 테스트를 포함해 Python 95건을 통과했다.

2026-08-07 제한형 실행 자원: 공용 자원 예산 서비스가 CPU 수와 가용 메모리로 I/O 스레드
2~8개, 대기 포함 I/O 작업은 4배, CPU 이미지 변환 프로세스 1~4개와 다운로드 대기열
1,000개를 정한다. 파일 검사·회차 이미지 목록·미리보기 디코딩은 GUI의 공용 QThreadPool로
옮겨 별도 Python 프로세스 생성을 없앴고 이미지 변환은 숨김 프로세스 슬롯을 넘지 못한다.
`performance resources --json`과 `status --json`의 `resources`가 같은 상한과 현재 사용량을
반환한다. 이 PC에서는 24논리 코어·가용 약 20GB로 I/O 8개, CPU 프로세스 4개가 선택됐으며
자원 예산·CLI·변환 프로세스 계약 테스트를 포함해 Python 97건을 통과했다.

### 단계 7. 배포와 사용 문서

목표: 개발 환경 지식 없이도 설치하고 실행할 수 있는 배포 형태를 만든다.

- [ ] 새 PC 기준 설치·실행 점검
- [ ] 개발자용 재현 가능한 설치 스크립트 정리
- [ ] PyInstaller GUI 실행 파일과 Node/Puppeteer 런타임 묶음 검토
- [ ] 업데이트 및 데이터베이스 마이그레이션 절차
- [ ] 오류 보고용 진단 정보 내보내기
- [ ] README의 GUI/CLI 명령과 실제 `--help` 일치 확인
- [ ] 릴리스 체크리스트와 버전 규칙

완료 조건:

- 깨끗한 Windows 환경에서 문서만 보고 설치와 실행이 가능하다.
- 사용자 데이터와 앱 실행 파일의 저장 위치가 분리된다.
- 진단 묶음에서 민감 정보가 제거된다.

## 6. 공통 완료 정의

기능 하나는 다음 조건을 전부 충족해야 완료로 표시한다.

- 실제 공용 서비스 로직이 구현됐다.
- 같은 기능을 수행하거나 검증하는 CLI가 있다.
- 성공과 실패의 종료 코드 및 JSON 결과가 정확하다.
- 관련 자동 테스트가 통과한다.
- GUI 동작이 실제 서비스에 연결됐다.
- CLI로 기능을 실행한 뒤 상태와 파일 결과를 검증했다.
- 필요한 경우 GUI 화면을 직접 캡처해 배치, 문구와 상태를 확인했다.
- 한글과 공백이 있는 Windows 경로를 검증했다.
- README, CLI 도움말과 계획서 상태가 같은 변경에서 갱신됐다.
- `python -m py_compile`, `node --check`, 테스트와 `git diff --check`가 통과했다.

## 7. 추진모드 운영 규칙

추진모드 지시를 받으면 다음 방식으로 작업한다.

1. 실제 작업 트리, 현재 브랜치, 실행 중인 GUI와 기존 데이터를 먼저 확인한다.
2. 이 계획에서 아직 완료되지 않은 가장 높은 우선순위 단계를 선택한다.
3. 기능을 너무 크게 묶지 않고 독립적으로 검증 가능한 수직 단위로 구현한다.
4. 각 수직 단위마다 서비스 → CLI → 테스트 → GUI → 캡처 → 문서를 완결한다.
5. 오류가 생기면 로그와 재현 명령으로 원인을 확인하고 가능한 범위에서 수정 후 계속한다.
6. 현재 단계 완료 후 다음 단계로 이어서 진행한다.
7. 검증을 통과한 논리적 단위마다 명확한 메시지로 커밋한다.
8. 사용자가 추진모드 프롬프트에서 푸시까지 허용했으면 현재 추적 브랜치에 푸시한다.

다음 경우에는 임의로 결정하지 말고 사용자에게 확인한다.

- 실제 다운로드 파일 삭제 또는 원본을 대체하는 변환
- 대량 폴더 이동처럼 실패 시 복구 비용이 큰 작업
- 개인 Chrome 프로필, 계정 쿠키 또는 외부 인증 정보 사용
- 지원 사이트의 접근 제한을 우회하는 방식 변경
- 기존 다운로드 결과의 폴더명 규칙이나 메타데이터 호환성을 깨는 변경
- 전체 기술 스택 교체 또는 배포 형태를 한 가지로 확정하는 결정

단순한 UI 배치, 내부 클래스 분리, 테스트 추가, 인덱스 추가처럼 되돌릴 수 있고 기존
요구사항 안에 있는 결정은 합리적인 기본값을 선택해 계속 진행한다.

## 8. 진행 보고 형식

중간 보고는 짧게 유지하고 다음 네 가지를 포함한다.

- 지금 구현하는 수직 기능 단위
- 확인된 문제 또는 선택한 기본값
- 통과한 CLI/자동 테스트와 GUI 캡처 여부
- 다음으로 이어갈 항목

최종 보고에는 완료 기능, 검증 결과, 남은 단계, 변경 파일, 커밋과 푸시 결과를 포함한다.
기능이 일부만 연결됐으면 완료라고 표현하지 않고 정확히 남은 부분을 기록한다.

## 9. 계획 상태 갱신 규칙

- 완료된 체크박스만 `[x]`로 바꾼다.
- 부분 구현은 체크하지 않고 해당 항목 아래에 현재 상태와 남은 일을 적는다.
- 새 요구사항은 가장 가까운 단계에 추가하고 CLI 대응과 완료 조건도 함께 적는다.
- 우선순위를 바꾸면 이유를 한 줄 기록한다.
- 계획서 자체와 실제 구현이 다르면 실제 코드를 검증한 뒤 계획서를 즉시 갱신한다.
