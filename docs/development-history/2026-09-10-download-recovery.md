# 2026-09-10 다운로드 실패 3종 수정과 이어받기

## 배경과 범위

- 시작 기준: `ec17b0c` (`codex/toki-gui-foundation`).
- 53개 작품 중 6개 실패를 실제 로그, 사이트 목록, 이미지 응답과 저장 상태로 진단했다.
- 사용자는 번호 없는 회차도 사이트 순서대로 다운로드하고, 실행 중 작업을 잠시 멈춘 뒤 수정본으로 이어받도록 요청했다.
- 진단 중에는 파일·설정을 수정하지 않았으며, `진행해` 승인 후 유지보수를 실행했다.

## 원인과 수정

### 번호 없는 회차 때문에 작품 전체가 중단됨

쿠로이와 메다카, 히토너, 원펀맨의 일부 실제 목록 제목은 작품명만 있거나 화수 없는 제목이었다.
기존 코드는 전체 회차 매핑을 만들 때 빈 회차 접미사를 오류로 처리해 정상 회차까지 시작하지 못했다.

- 제목이 존재하면 회차 번호가 없어도 허용한다. 비어 있는 목록 제목 자체는 계속 오류로 처리한다.
- 기존 6자리 사이트 순번을 정렬 기준으로 사용한다. 예: `000001 히토너`, `000011 원펀맨 리메이크`.
- 임의의 화수를 만들지 않고 `sourceTitle`, `sourceId`, `sourceUrl`을 보존한다.
- 같은 제목인 두 회차도 각 순번과 고유 ID로 구분한다. 소수·분할 형제 회차 규칙은 유지한다.
- Python 회차 폴더 정리 미리보기도 같은 정책을 사용한다. 순번 없는 레거시 제목 전용 이관은 충돌을 막기 위해 여전히 차단한다.
- 이번 유지보수에서는 기존 사용자 회차 폴더의 일괄 변경을 실행하지 않았다.

### 정상 JPEG 뒤 부가 데이터를 손상으로 오판

울퉁불퉁 마녀 모녀의 사정 25화의 `image0024.jpg`는 JPEG 종료 마커 뒤에 46바이트가 붙어 있었다.
파일은 641,788바이트이고 실제 디코딩 결과는 1512×2150이었다. 기존 검사는 마지막 2바이트만 확인해 거부했다.

- 일반 JPEG는 기존의 가벼운 헤더·끝부분 검사 경로를 유지한다.
- 끝이 특이한 경우에만 마지막 64KiB에서 종료 마커를 찾고 엄격한 전체 디코딩을 수행한다.
- `jpeg-js@0.4.4`를 고정 의존성으로 추가했다. 입력 128MiB, 해상도 64MP, 디코더 메모리 256MiB 상한을 적용한다.
- 이 검사는 GUI 스레드가 아니라 Node 다운로더에서 실행한다. 원본 바이트는 자르거나 변환하지 않는다.
- 다운로드 저장과 기존 파일 재사용 검사에 동일하게 적용한다. 가짜 종료 마커나 잘린 이미지가 기존 정상 파일을 덮어쓰지 않도록 테스트했다.
- 디코더 옵션 기준: [jpeg-js 공식 문서](https://github.com/jpeg-js/jpeg-js).

문제 이미지 SHA-256: `f784c7fa5d907388c4b21dc6381cb36d1a5bfba88ebb8061275738d50b60a0c4`.

### OneDrive 경로의 진행 기록 교체가 일시적으로 거부됨

TSUYOSHI와 일일외출록 반장은 회차 이미지 저장 후 `.toki-state.json` 교체에서 `EPERM`으로 중단됐다.
권한과 읽기 전용 속성은 정상이었고 OneDrive 동기화 경로였지만, 과거 잠금의 주체를 특정한 것은 아니다.

- `downloader_files.js` 공용 파일 교체 재시도를 추가했다.
- `EPERM`, `EACCES`, `EBUSY`에 한해 최대 9회 시도한다. 대기는 100/200/400/800/1000/1000/1000/1000ms, 총 5.5초다.
- 비동기 대기로 이벤트 루프를 막지 않는다. 디스크 용량 부족 등 다른 오류는 즉시 보고한다.
- 진행 기록은 PID와 UUID가 포함된 새 임시 파일에 쓰고 fsync 후 교체한다. 이전 `.tmp` 파일을 덮어쓰지 않는다.
- 교체 실패 시 기존 진행 기록을 삭제하지 않으며 새 임시 기록 경로도 오류 진단에 남긴다.
- 이미지 교체도 같은 재시도를 사용한다. 기존 정상 파일을 먼저 삭제하는 우회는 사용하지 않는다.
- 실제 권한 부족이나 계속 유지되는 잠금은 이 재시도만으로 해결되지 않는다.

## CLI 검증

```powershell
node --test tests/*.test.js
.\.venv\Scripts\python.exe -X utf8 -m unittest discover -s tests -p 'test_*.py'
```

- Node 78개, Python 448개: 총 526개 통과.
- 일시 잠금 후 성공, 영구 잠금의 제한된 대기와 기록 보존, 비재시도 오류, 이전 임시 파일 보존을 주입 테스트했다.
- 실제 문제 JPEG를 기존 인증서 검증을 유지하는 이미지 전송 경로로 다시 받아 동일 SHA-256 및 `valid: true`를 확인했다.
- 실제 CLI 메타데이터 검증: 히토너 13개/1페이지, 쿠로이와 223개/3페이지, 원펀맨 357개/4페이지를 모두 수집했다.
- 실제 회차 다운로드: 히토너 순번 1의 58장, 쿠로이와 순번 1의 33장, 원펀맨 순번 11의 16장을 다운로드 완료했다.
- 실제 사이트 테스트는 사용자 저장 폴더가 아닌 `logs/fix-probes-20260910`에 격리했다. 샘플 파일과 원본 이미지는 Git에 포함하지 않는다.

재현 예시(원펀맨은 `-start 11 -last 11`):

```powershell
node down.js -url "https://newtoki1.org/manhwa/35040" -scan-mode range -start 1 -last 1 -output "C:\gitproject\tokiDownloader\logs\fix-probes-20260910"
```

## 실행 중 작업 보존과 복원

1. 봉신연의 `bd56597005`를 21권 이미지 83/91에서 CLI 일시정지했다.
2. 53개 작품·64개 실행 기록, 설정, 현재 작업과 대기 13개의 순서를 `logs/maintenance-20260910-*`로 백업했다.
3. 클립보드 감시를 임시로 끄고 기존 GUI/다운로더를 종료한 뒤 수정본을 실행했다.
4. 봉신연의를 같은 작품·폴더로 재등록하고 기존 대기 13개를 같은 순서와 옵션으로 복원했다. 러브 다이어리의 `full` 검사 모드도 유지했다.
5. 실패 6개는 원래 대기열 뒤에 재시도하도록 등록했다. 기존 작품 행을 재사용하여 총 작품 수는 53개다.
6. 클립보드 감시·자동 다운로드를 원래 설정으로 복원했다. 작품 전체 압축과 원본 정리 설정은 변경하지 않았다.
7. 봉신연의 새 실행 `f7f7685dfe`가 기존 완료 20권을 건너뛰고 21권을 마친 뒤 22권으로 진행한 것을 로그와 CLI 상태로 확인했다.

복원 검증은 대기열 19개의 작품 키, 순서, URL, 저장 경로, 검사 모드, 범위, 동시성, 재시도 설정, 메모·고정을 백업과 비교했다.
복원 후 스냅샷은 `logs/maintenance-20260910-restored-jobs.json`, GUI 자체 캡처는 `logs/2026-09-10-download-recovery.png`다.
작품 전체 재시도 6개는 원래 대기 작업 이후 실행되므로, 이 기록 시점에 전체 다운로드 완료까지 검증했다는 뜻은 아니다.

## 후속: 은혼 563화의 사이트 이미지 준비 중 안내 처리

이 절은 `707d895` 당시 처리 정책이다. 이후 사용자 요청으로 완료/압축 정책은 아래의
"작업 완료와 미수신 회차 분리" 절로 변경했다. 원본 준비 중 감지와 미수신 ID 보존 규칙은 유지한다.

### 실제 원인

`3337105` 반영 후 은혼은 711개 중 561개 항목을 완료하고, 사이트 순번 562인 **은혼 563화**에서 실패했다.
16:52~16:55 로그에서 본문 이미지 대기 60초가 최초 실행과 자동 재시도 2회 모두 반복됐다.
해당 페이지(`/manhwa/40/1674773`)는 본문 대신 `이미지 처리 중인 회차입니다. 잠시 후 다시 확인해주세요.`라는 안내를 반환했다.
앞뒤 회차인 은혼 562화와 564화는 각각 이미지 17개를 위한 본문 데이터를 가지고 있었다.
즉 파일 잠금·이미지 형식 문제와 별개의 사이트 원본 준비 상태였으며, 기존 코드는 안내를 인식하지 못하고 작품 전체를 멈췄다.

### 처리 계약

- `downloader_episodes.js`에서 본문 이미지 또는 **관찰된 정확한 준비 중 안내**를 기다린다.
- 이미지가 존재하면 이미지를 우선한다. 인증 오류, 일반 로딩 문구, 알 수 없는 빈 페이지는 준비 중으로 추정하지 않는다.
- 준비 중이면 고유 회차 ID, 원래 제목·URL, 사유, 확인 시각을 `pendingEpisodes`에 즉시 저장하고 다음 회차를 계속한다.
- 해당 ID를 완료 집합에 추가하지 않는다. 나중에 신규 검사에서 다시 선택되며 실제 성공하면 미완료 기록을 제거한다.
- 범위 밖에 남은 미완료 기록도 보존한다. 이전에 완료한 로컬 회차는 전체 검사 중 원본이 준비 중이어도 지우거나 완료 취소하지 않는다.
- 미완료가 남으면 마지막에 `episodes_pending` / `source` / `retryable: false` 오류를 보고한다. GUI의 기존 오류 상태에서 `원본 상태` 및 `일부 미완료` 이유를 표시한다.
- 정상 `completed` 이벤트를 내지 않으므로 자동 압축·원본 정리·자동 PDF는 실행하지 않는다. 전체 작품의 100% 완료로 표시하지 않는다.
- GUI에 `episode_deferred` 기록을 연결하고 진행률·실행 기록은 보류 항목을 성공 건수에서 제외한다. 즉각적인 전체 반복 재시도 대신 다음 작품을 진행한다.
- 사이트가 이미지를 실제 제공하기 전에는 해당 회차 자체를 받을 수 없다. 자동 장기 감시는 추가하지 않았다.

### CLI 검증과 운영 복원

- Node 83개, Python 449개: 총 **532개 통과**.
- 새 테스트: 정확한 안내 식별, 일반 빈 페이지/인증 오류 유지, 브라우저 대기 핸들 해제, 미완료 ID 저장·재선택·해소, 범위 밖 미완료 보존.
- GUI 테스트: 두 회차 중 첫 회차 보류/다음 회차 성공 시 성공 건수 1, 진행률 50%, 원본 상태 오류 종료, 자동 압축 미호출, 자동 재시도 없이 다음 작품 예약.
- 실제 CLI:

```powershell
node down.js -url "https://newtoki1.org/manhwa/40" -scan-mode range -start 562 -last 563 -output "C:\gitproject\tokiDownloader\logs\gintama-processing-probe-20260910" -json-events
```

- 실제 결과: 은혼 563화 즉시 보류 → 은혼 564화 17장 완료 → `episodes_pending`, 종료 코드 1. 이 종료 코드는 미완료를 성공으로 오인하지 않기 위한 의도된 결과다.
- 저장 상태 검증: 완료 ID는 `/manhwa/40/1674774`만 포함, 미완료 ID는 `/manhwa/40/1674773`, 다음 신규 검사에서 후자만 선택. 빈 가짜 완료 폴더는 만들지 않았다.
- 사용자 승인 후 일곱개의 대죄 순번 193에서 일시정지하고 현재 작품·대기 15개·설정을 `logs/maintenance-gintama-20260910-before.json`에 보존했다.
- 작품 53개·실행 84개의 원본 스냅샷은 `logs/maintenance-gintama-20260910-jobs.json`이다.
- 수정본 실행 후 현재 작품을 우선 이어받고, 은혼 재시도를 그다음에 배치한 뒤 기존 대기 15개의 상대 순서와 실행 옵션을 복원했다. 작업 행 수 53개 유지와 클립보드 설정 복원을 CLI로 검증했다.
- 복원 후 스냅샷은 `logs/maintenance-gintama-20260910-restored.json`이다. 기존 다운로드 파일 삭제나 폴더 이관은 실행하지 않았다.
- 실제 복원 로그에서 기존 완료 193개를 건너뛰고 순번 194부터 이어받아 197까지 진행한 것을 확인했다. 자체 캡처 `logs/2026-09-10-gintama-processing-fix.png`에서도 실행 1개·대기 16개·총 작품 53개를 확인했다.

## 작업 완료와 미수신 회차 분리

사용자는 받을 수 있는 회차를 처리했다면 작품 작업은 완료로 끝내고, 받은 회차를 압축하기를 원했다.
따라서 `707d895`의 미수신→전체 오류 정책을 다음처럼 변경했다.

- Node는 정상 `completed` 이벤트와 종료 코드 0을 보낸다. 미수신 수·목록·안내는 별도 필드로 전달한다.
- GUI/DB 기본 상태는 `완료`로 유지하고, 리스트·그리드·상세·실행 이력·CLI에 `완료 · 미수신 N개`를 표시한다.
- `pending_episode_count`, `completion_note`를 작품/실행 JSON payload에 저장한다. 기존 DB 행은 기본값 0/빈 문자열로 읽으므로 테이블 변경은 필요 없다.
- 목록 렌더링 중 파일이나 ZIP을 열지 않는다. 저장된 미수신 수로 O(1) 표시하며 완료 필터와 작업 예약 로직을 유지한다.
- 100%는 **이번 작업 처리 완료**다. 실제 수신 회차 수와 미수신 수를 합쳐서 성공 건수를 부풀리지 않는다.
- 자동 압축을 정상 실행한다. ZIP에는 기존 메타데이터와 함께 `.toki-state.json`을 넣는다. 두 파일은 원본 정리 대상이 아니다.
- 미수신 ID를 완료/압축 회차 목록에 넣지 않는다. 나중에 해당 회차를 받은 뒤 기존 ZIP을 갱신하면 회차 순서대로 삽입되고 나머지 이미지 바이트는 유지된다.
- 미수신이 해결되면 표시와 기록의 미수신 수를 0으로 지운다. 인증/네트워크/파일 오류는 여전히 정상 완료로 바꾸지 않는다.

### 검증

- Node 84개, Python 451개: 총 **535개 통과**. 처리 대상 전부가 준비 중인 경우도 작업 완료 요약과 실제 성공 수 0을 구분한다.
- GUI 테스트: 첫 회차 보류 후 다음 회차 성공 → 처리 중 50%/성공 1개 → 최종 완료 100%/미수신 1개 → 자동 압축 예약.
- 실제 CLI 격리 테스트: 완료·미수신 수의 DB/실행 이력/`info --json` 보존, 기존 2회차 ZIP 생성과 원본 정리, 미수신 기록의 ZIP 내·외 보존, 나중에 빠진 회차를 동일 ZIP 중간에 삽입, 기존 이미지 바이트 보존.
- 실제 은혼 두 항목 범위 CLI: 563화 보류, 564화 기존 이미지 재사용, `완료 · 미수신 1개` 요약 및 종료 코드 0 확인.

### 유지보수 상태 보존

- 시작 시 실행 중 작품과 대기 13개, 설정을 `logs/maintenance-partial-complete-20260910-before.json`에 보존했다.
- 전체 53작품·101실행 스냅샷은 `logs/maintenance-partial-complete-20260910-jobs.json`이다.
- 일시정지 직전 네토라세라레는 첫 회차를 마치고 두 번째 회차를 진행했다. 종료 당시 중쇄를 찍자!의 ZIP은 이미 검증·생성됐고 원본 정리만 취소되어 ZIP과 남은 원본이 보존됐다. 이 정리는 수정 후 기존 설정대로 재개한다.
- 실제 은혼 재검사(`fe499c76f2`)는 완료 710개를 재사용하고 563화만 보류한 뒤 17:53:15에 `완료 · 미수신 1개`, 100%, 종료 코드 0으로 끝났다. 전체 ZIP 자동 압축이 시작되고 원래 실행 작품과 대기 13개는 기존 상대 순서로 진행한다.
- GUI 자체 캡처 `logs/2026-09-10-completed-with-missing.png`와 실제 `info`/`run-info` 출력에서 동일한 완료·미수신 표시를 확인했다. 캡처용 검색 필터는 원래 전체 목록으로 복원했다.
- 복원 스냅샷 `logs/maintenance-partial-complete-20260910-restored.json`: 53작품·116실행. 클립보드와 기존 압축 후 원본 정리 설정은 유지했다.
- 별도 발견 사항: 중쇄를 찍자!의 원본 정리 재개는 남은 이미지 675개를 검증된 ZIP에 보존하고 정리했으나, 빈 회차 폴더 삭제에서 WinError 5로 끝났다. ZIP은 보존되고 다운로드는 계속된다. 이 빈 폴더 정리 권한 문제는 이번 미수신 완료 정책 수정과 별개이며 남은 제한으로 기록한다.

## 후속: 러브 다이어리의 손상 이미지로 인한 전체 중단 방지

### 확인된 원인

- 실행 `c3e97f4277`은 2026-09-10 18:17:33에 사이트 순번 32, 원래 제목
  `러브다이어리 (후타…러브다이어리 28`에서 이미지 85장 중 2장 검증 실패로 중단됐다.
- 문제 주소 `https://aws-cdn9.site/manhwa/3184/12176/p002.jpg`는 HTTP 200,
  `Content-Type: image/jpeg`지만 실제 바이트 시그니처는 BMP였다.
- 서버가 선언한 HTTP 길이와 받은 크기는 모두 3,342,336바이트로 일치했다.
  BMP 내부 헤더는 968×1414, 24bpp, 4,106,310바이트를 요구하며 Pillow 전체 해독도
  `image file is truncated`로 실패했다. 단순 확장자 변경으로 해결되는 정상 이미지가 아니다.
- 반복 응답 SHA-256은 `73cfba49e242ce96df64a9ce61842b2e3ecd54cb9a7010654948ffd0b84dca8c`로 같았다.
  사이트 원본의 현재 상태를 확인한 것이며 과거 업로드 손상의 발생 원인까지 확인한 것은 아니다.
- 해당 회차의 정상 이미지 83장과 이전 완료 31개 회차는 그대로 있었다.

### 변경 계약

- 이미지 요청 3회가 **모두 알려진 이미지 형식의 검증 실패**일 때만 회차 보류가 가능하다.
  확장자/형식 불일치, JPEG/PNG 끝 표식 누락, JPEG 해독 실패, WebP 크기/청크 오류를 구분한다.
- 회차 내부 병렬 작업이 모두 끝난 후 오류를 모은다. 손상 이미지 외에 인증·통신·저장 오류가
  하나라도 섞이면 보류하지 않고 오류를 보고한다. HTML/빈 응답이나 검증 자원 한도 초과도 제외한다.
- `pendingEpisodes`의 사유는 `source_image_validation_failed`이며 `failedImages`에
  각 이미지 URL, 파일명, 검증 사유, 기대/검출 형식, 크기, 시도 횟수를 보존한다.
  이전에는 첫 오류만 문자열로 남아 다른 실패 이미지와 분류 정보가 사라졌다.
- 불완전한 회차 ID/순번은 완료 집합에서 제외하고 상태를 저장한 뒤 다음 회차를 처리한다.
  이번 검사 전에 완료 기록이 있었더라도 실제 이미지 재검증이 실패한 회차를 완료로 두지 않는다.
- 다운로드된 정상 이미지나 기존 파일은 삭제하지 않는다. 정상 재시도 응답이 확인되기 전에는
  파일 교체도 하지 않는다. 이후 신규 검사에서 미수신 회차를 다시 선택하고 정상 이미지는 재사용한다.
- 작업 종료는 기존 `완료 · 미수신 N개`를 사용한다. 받은 **완전한 회차**만 전체 ZIP에 포함하고,
  미수신 회차의 부분 이미지 폴더는 압축 후 원본 정리에서 제외한다.
- GUI의 중간 안내도 실제 보류 사유를 받아 표시한다. 손상 이미지를 무조건 '사이트 이미지 준비 중'으로
  표시하지 않는다. 목록 표시에서 파일을 읽는 추가 작업은 없다.

### 검증과 실제 재개

- Node 90개, Python 452개: 총 **542개 통과**.
- Node 추가 검사: 3회 재시도, 모든 실패 URL 보존, 기존 파일 보존, 중간 재시도 성공,
  HTML/빈 응답/403/429/통신 오류 제외, 손상+운영 오류 혼재, 실제 로컬 저장 실패 제외,
  보류 후 다음 회차 성공·진행 기록 복원·신규 검사 재선택·복구 후 미수신 해소.
- GUI 검사: 이미지 검증 실패 안내와 미수신 수가 작품/실행 기록에 전달되고 종료 후 자동 압축 예약.
- 압축 검사: 앞/뒤 정상 회차만 ZIP에 저장·정리하고, 중간 미수신 회차의 부분 이미지와
  `failedImages` 기록을 디스크 및 ZIP 진행 기록에 보존.
- 유지보수 시작 시 실행/대기/압축 작업은 없었다. 53작품·116실행을
  `logs/love-diary-before-20260910.json`, 설정을 `logs/love-diary-config-before-20260910.json`에 백업했다.
- GUI를 안전하게 재시작하고 CLI `retry --job c3e97f4277`로 동일 작품·동일 폴더를 재사용하는
  새 실행 `e9e69b9242`를 시작했다. `full` 검사, 숨김 브라우저, 동시 이미지 5개, 기존 압축 설정을 유지했다.
  재시작 중 잠시 끈 클립보드 감시/자동 다운로드는 원래 활성 설정으로 복원했다.
- 실제 22:45:54 로그에서 순번 32의 `p001.jpg`, `p002.jpg`를 각각 3회 시도한 뒤 보류하고,
  22:45:57에 순번 33으로 진행했다. 순번 33의 89장이 완료되고 22:46:15에 순번 34를 시작했다.
  상태 JSON에서 미수신 ID가 완료 집합에 없고 기존 부분 이미지 83장이 남았음을 확인했다.
- `logs/2026-09-10-love-diary-image-deferral.png` 자체 캡처에서 **실행 중 · 미수신 1개**,
  다음 회차 진행과 두 실패 URL 로그를 확인했다. 목록 검색/정렬은 변경하지 않았다.
- 복원 스냅샷 `logs/love-diary-resumed-20260910.json`: 기존 53작품 유지, 실행 이력만 117개로 증가.
  이 검증 시점에는 후속 회차 다운로드가 진행 중이며 작품 전체 완료나 실제 최종 ZIP 생성을
  확인했다는 뜻은 아니다. 기존 OneDrive 빈 폴더 정리 권한 문제는 이번 수정 범위에 포함하지 않았다.
